# AI 编程工程化基线（AI 宪法）— 研究/模型项目

> 适用：算法、模型训练、数据处理、benchmark 等 Python 研究型项目。
> 铁律：**实验可迭代，但代码必须可复现、可测试、可追溯。禁止"跑通就提交"。**

## 0. 总则
1. 研究代码同样工程化：可复现（seed/版本/数据固定）、可测试、可追溯。
2. 禁止 Vibe Coding：禁止随手改、无注释、无测试。
3. Notebook 用于探索，**正式逻辑必须沉淀为 .py 模块 + 测试**。

## 1. 技术栈
| 层级 | 技术 |
| --- | --- |
| 语言 | Python ≥ 3.11 |
| 依赖管理 | pyproject.toml / uv |
| 测试 | pytest |
| Lint | ruff + mypy |
| 计算 | 按项目：torch / jax / numpy / pandas |
- 依赖变更记录原因与版本。

## 2. 目录结构（建议）
```
src/ 或 包名/      核心模块：模型/数据处理/训练/评估
adapters/          外部模型/数据源适配
benchmarks/        基准测试
scripts/           可运行入口脚本（训练/评估/导出）
tests/             单元测试
notebooks/         探索用（不进生产路径）
data/ checkpoints/  数据与权重（gitignore，记录来源与版本）
```

## 3. 可复现性强制
1. 所有随机源必须设 seed（random/numpy/torch）。
2. 数据处理必须有明确 pipeline，禁止不可追溯的临时清洗。
3. 模型权重/数据集记录版本与来源（README 或 manifest）。
4. 训练/评估脚本必须可通过命令行复现（参数化，非硬编码路径）。

## 4. 代码规则
1. 单一职责，单函数 ≤ 80 行，嵌套 ≤ 3 层。
2. 禁止魔法值（学习率/批次/维度抽常量或 config）。
3. 配置与代码分离（yaml/dataclass/config 文件）。
4. 所有 I/O（文件/模型加载）有路径校验与异常处理。
5. 禁止 print 进模块，用 logging；Notebook 除外。

## 5. 测试
1. 数据变换、模型前向、指标计算必须有单元测试（给定输入断言输出）。
2. 关键数值用 assert 验证范围/形状/有限性（防 NaN/Inf）。
3. benchmark 必须有基线对比与回归检测。

## 6. 安全
1. 加载外部权重/数据校验来源与完整性。
2. 禁止硬编码路径、密钥、API key。
3. 大文件走 gitignore + manifest。

## 7. AI 专属禁止清单
1. 禁止擅自改实验结论/捏造结果。
2. 禁止 silently 改 seed 或数据以"跑通"。
3. 禁止删测试以通过 CI。
4. 禁止在正式模块留探索性 hack 而无注释。

## 元规则
- 本文件与现有代码规范冲突时以现有代码为准；与 Skill 冲突时以本文件为准。
